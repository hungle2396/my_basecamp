<%= form_with(model: [@discussion, @comment], local: true, class: "comment_form") do |form|%>
<%= form.text_field :body, class: "comment_field"%>
<%= form.submit "Comment", class: "comment_submit" %>
<% end %>